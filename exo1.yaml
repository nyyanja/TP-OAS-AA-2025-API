openapi: 3.0.3
info:
  title: API Ping
  version: 1.0.0
servers:
  - url: 'http://localhost:8080'

paths:
  /ping:
    get:
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: "pong"